// Overwritten by the frontend container's entrypoint from API_BASE_URL. This default is the one
// the Angular dev server uses, where the backend runs on the host's port 8080.
window.__env = { apiBaseUrl: 'http://localhost:8080/api' };
